<div class="dashboard-container d-flex">
  <!-- Sidebar -->
  <div class="sidebar p-3">
    <ul class="nav flex-column">
      <!-- Projects Dropdown -->
      <li class="nav-item">
        <button class="btn btn-outline-primary w-100" type="button" data-bs-toggle="collapse" data-bs-target="#projectsDropdown">
          📂 Projects
        </button>
        <ul id="projectsDropdown" class="collapse mt-2">
        <li>
          <%= link_to "Ongoing", freelancer_shared_projects_path(status: 1), class: "dropdown-item",
                data: { turbo_frame: "projects_frame" } %>
        </li>
          <li><%= link_to "Pending", freelancer_shared_projects_path(status: 0), class: "dropdown-item", data: { turbo_frame: "projects_frame" } %></li>
          <li><%= link_to "Completed", freelancer_shared_projects_path(status: 4), class: "dropdown-item", data: { turbo_frame: "projects_frame" } %></li>
        </ul>
      </li>

      <!-- Messages -->
      <li class="nav-item">
        <button class="btn btn-outline-success w-100 mt-2">
          💬 Messages
        </button>
      </li>

      <!-- Profile Dropdown -->
      <li class="nav-item">
        <button class="btn btn-outline-secondary w-100 mt-2" type="button" data-bs-toggle="collapse" data-bs-target="#profileDropdown">
          👤 Profile
        </button>
        <ul id="profileDropdown" class="collapse mt-2">
          <%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item",
          data: { turbo_frame: "projects_frame" } %>

          <%= link_to "Create Profile", new_freelancer_profile_path, class: "dropdown-item",
          data: { turbo_frame: "projects_frame" } %>
        </ul>
      </li>
    </ul>
  </div>

  <!-- Main Content -->
  <main class="dashboard-content flex-grow-1 p-4">
  <turbo-frame id="projects_frame">
    <%= render partial: "projects_list", locals: { shared_projects: @shared_projects } %>
  </turbo-frame>
  </main>

</div>

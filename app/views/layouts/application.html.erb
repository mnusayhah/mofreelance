<!DOCTYPE html>
<html>
  <head>
    <title>Mofreelance</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!--Added for Bootstap behaviours like collapse-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- CSS compilé localement, incluant Bootstrap (via Sass) et tes surcharges Malt -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- JavaScript via importmaps -->
    <%= javascript_importmap_tags %>
    <!-- Ton JS compilé localement (packs ou autre) -->
    <!-- <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %> -->
    <%= turbo_include_tags %>
  </head>

  <body class="bg-light">
    <!-- Barre de navigation -->
    <%= render "shared/navbar" %>

    <!-- Flash messages -->
    <%= render "shared/flashes" %>

    <!-- Contenu principal -->
    <%= yield %>
  </body>
</html>
